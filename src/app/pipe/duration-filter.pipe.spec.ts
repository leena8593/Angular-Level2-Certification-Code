import { DurationFilterPipe } from './duration-filter.pipe';

describe('DurationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
